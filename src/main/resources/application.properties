spring.application.name=tcp-log-server

# TCP Server Configuration
tcp.server.port=9999
tcp.server.backlog=100

# Maximum message size in bytes before CRLF delimiter must be found
# Default: 65536 (64KB) - increase if you need to receive larger log messages
tcp.server.max-message-size=65536
